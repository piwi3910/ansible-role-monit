check process gitea with match gitea
    start program = "/bin/systemctl start gitea" with timeout 60 seconds
    stop program  = "/bin/systemctl stop gitea"
    if cpu usage > 80% for 5 cycles then alert
  	if failed host 127.0.0.1 port 3000 then restart
	if 5 restarts within 5 cycles then timeout
