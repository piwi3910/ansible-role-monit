check process Loginsight_agent with pidfile /var/run/liagent/liagent.pid
    start program = "/bin/systemctl start liagentd" with timeout 30 seconds
    stop program  = "/bin/systemctl stop liagentd"
    if cpu usage > 80% for 5 cycles then alert
	if 5 restarts within 5 cycles then timeout